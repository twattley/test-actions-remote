name: 'Python test'
description: 'description'
inputs:
  github-pat:
    description: 'github persoanl access token'
    required: true
  test-variable:
    description: 'Initial test variable'
    required: true


runs:
  using: "composite"
  steps:
    - name: PR Check
      if: ${{ github.event_name == 'push' }}
      run: echo "push check from nested action"
      shell: bash

