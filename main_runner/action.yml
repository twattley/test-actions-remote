name: Common Action

on:
  - push
  - pull_request

jobs:

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./helper_action
      - uses: ./helper_action_two