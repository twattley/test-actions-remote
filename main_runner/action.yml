name: Common Action

on:
  - push
  - pull_request

runs:
  using: "composite"
  steps:
    - name: Check Common Actions Repository
      uses: actions/checkout@v3
      with:
        repository: twattley/test-actions-remote
        token: ${{ inputs.github-pat }}
        path: test-actions-remote
    - uses: test-actions-remote/helper_action
    - uses: test-actions-remote/helper_action_two