name: 'Python test'
description: 'description'
inputs:
  test-variable:
    description: 'Initial test variable'
    required: true

runs:
  using: "composite"
  steps:
  
    - name: hello
      if: ${{ github.event_name == 'push' }}
      run: |
        echo "Hello from inside a co action"
        chmod +x  test-actions-remote/helpers/*
        ./test-actions-remote/helpers/hello.sh  ${{ inputs.test-variable }}
      shell: bash